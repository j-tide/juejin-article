# Agent 系统工程 · 截至第 06 篇的配套实验

Python 3.10+，只使用标准库。当前可运行第 02—06 篇的本地机制实验。

在本目录执行，例如：

```bash
python3 run.py 06 --output results/06.json
```

`lab/` 保存截至本章的实现，`results/` 保存实际输出。各篇另附独立的累计 `code.zip`；第 01 篇的原始程序与单元测试在该篇目录。

没有接入真实模型、检索或外部交付服务。固定策略和教学数据用于检查机制，不能当作模型能力或任务成功率。各项实验的假设、取舍和未覆盖范围见正文。

[返回专栏目录](../README.md)
